V = 5

def prim(graph):
    selected = [False] * V
    selected[0] = True

    print("Edge : Weight")

    for _ in range(V - 1):
        min_edge = 999
        x = y = 0

        for i in range(V):
            if selected[i]:
                for j in range(V):
                    if not selected[j] and graph[i][j]:
                        if graph[i][j] < min_edge:
                            min_edge = graph[i][j]
                            x, y = i, j

        print(x, "-", y, ":", graph[x][y])
        selected[y] = True


graph = [
    [0, 2, 0, 6, 0],
    [2, 0, 3, 8, 5],
    [0, 3, 0, 0, 7],
    [6, 8, 0, 0, 9],
    [0, 5, 7, 9, 0]
]

prim(graph)
