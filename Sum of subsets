#include <stdio.h>

int set[]={5,10,12,13,15,18};
int n=6,target=30;

void subset(int i,int sum){
    if(sum==target){
        printf("Subset found\n");
        return;
    }
    if(i>=n || sum>target) return;

    subset(i+1,sum+set[i]);
    subset(i+1,sum);
}

int main(){
    subset(0,0);
}
